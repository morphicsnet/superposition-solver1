name: Security (placeholder)
on:
  push:
  pull_request:
  schedule:
    - cron: "0 3 * * 1"
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "TODO: add security checks (SAST, dependency scanning)"
